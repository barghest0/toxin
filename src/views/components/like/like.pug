mixin like({ name = '', count = 0, state = '' } = {})
  - const classes = state === 'checked' ? 'like__custom-input_active' : '';

  label.like
    input.like__input(type="checkbox", name=name, checked=state ? state : null)
    span.like__custom-input.js-like__custom-input(class=classes)= count
