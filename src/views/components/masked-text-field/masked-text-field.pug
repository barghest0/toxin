mixin masked-text-field(props = {})
  -
    const {
      label = '',
      name = '',
      placeholder = '',
      type = 'text',
      value = ''
    } = props

  .masked-text-field
    .masked-text-field__label= label
    input.masked-text-field__input.js-masked-text-field(
      type=type,
      name=name,
      id=id,
      placeholder=placeholder,
      value=value
    )
