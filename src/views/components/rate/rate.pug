mixin rate({name='rate', rate='1'}={})
  label.rate
    each val in [1, 2, 3, 4, 5]
      if val === rate
        input.rate__input(type='radio', name=name, value=val, checked)
      else
        input.rate__input(type='radio', name=name, value=val)
