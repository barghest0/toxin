
mixin radio-list({items=[]})
        .radio-list
            each item in items
                label.radio-list__item
                    if item['state'] === 'checked'
                        input(type="radio" name=item['name'] id=item['id'] checked).radio-list__input
                    else
                        input(type="radio" name=item['name'] id=item['id']).radio-list__input
                    span.radio-list__custom-input
                    .radio-list__title
                        +body-text({text: item['text']})
                

